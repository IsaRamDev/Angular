import { PipeUpperPipe } from './pipe-upper.pipe';

describe('PipeUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
